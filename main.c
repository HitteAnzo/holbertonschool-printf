#include "main.h"

/**
 * main - check the code
 * 
 * Return: Always 0
 */

int main(void)
{
    char chiffre = "50";
    char ligne = "Quel âge avez-vous?";
    char lettre = "A"
    _printf("%d, %s, %c", chiffre, ligne, lettre);
    return(0);
}